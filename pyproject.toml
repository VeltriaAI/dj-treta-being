[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"

[project]
name = "djclaw"
version = "5.0.0"
description = "Install your own AI DJ Being — powered by Google ADK"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = [
    "google-adk",
    "litellm",
    "httpx",
    "pyyaml",
    "textual",
    "rich",
    "google-genai",
    "mutagen",
    "librosa",
]

[project.scripts]
djclaw = "cli:main"

[tool.setuptools]
py-modules = ["cli", "tui"]

[tool.setuptools.packages.find]
include = ["agent*"]
